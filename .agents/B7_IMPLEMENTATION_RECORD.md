# B7 plain recurrent PPO implementation record

Date: 2026-07-14 (Asia/Singapore)

## Scope

The implementation is isolated to new B7 modules, scripts, tests and
prospective documentation. Frozen B4/B5/B6 learner and evidence sources were
not edited.

Implemented surfaces:

```text
bplus_v22/b7_recurrent.py
bplus_v22/b7_env.py
bplus_v22/b7_eval.py
scripts/run_b7_recurrent.py
scripts/run_b7_eval.py
tests/test_b7_recurrent.py
tests/test_b7_eval.py
```

The deployment snapshot is always `policy.actor.state_dict()`. Strict loading
requires the canonical 12-key `End2Race` inventory; critic, fixed std,
optimizer, replay and hard-queue state remain training-only.

## Corrections made while translating the owner proposal

1. Collision redistribution is described only as discount-equivalent from the
   beginning of the one-second window and earlier, not at every state inside
   the window.
2. The current-rollout safe cap is explicitly scoped to observation histories
   generated by the pre-update policy. It does not claim to constrain the
   candidate's next closed-loop state distribution.
3. Old-policy rollout KL is the exact nonnegative fixed-variance Gaussian
   mean-KL; raw-action PPO ratios still use stored old/new log-probability.
4. Full recurrent replay is frame-by-frame from zero hidden. A fused GRU call
   is not used because it can differ numerically from the evaluator's batch-one
   execution.
5. The 288 panel's directional cluster is L4, not Austin startpoint. The exact
   one-sided sign-flip tail is computed by dynamic programming.
6. The absolute `collision<=18` and paired `fixed-new>=6` checks are retained
   for reporting but explicitly marked algebraically redundant at BC=24.
7. Hard mining is frozen to the immediately preceding iteration; missing slots
   use hash-selected BC-overtake preservation rows. This avoids a stale
   candidate-outcome cache while keeping every current rollout on-policy.
8. Seed0 CLI execution fails closed without a passed B7 seed1 288 summary.

## Validation before commit

The following ten standalone programs passed:

```text
test_bplus_v22_exploration.py
test_bplus_v22_objective.py
test_bplus_v22_ppo.py
test_bplus_v22_ppo_buffer.py
test_bplus_v22_ppo_env.py
test_bplus_v22_remediated_model.py
test_bplus_v22_ppo_runner.py
test_bplus_v22_ppo_eval.py
test_experiment_runner.py
test_b7_recurrent.py
```

`test_b7_eval.py` separately passed the exact 288-row inventory, paired
fixed/new calculation, absolute KPI, L4 sign-flip and continue-gate regression.
All new modules/scripts passed `py_compile`, both CLIs dry-loaded, and
`git diff --check` passed.

Production-shaped local GPU evidence (correctness only, no candidate KPI
comparison):

- the real 1,640-row selector produced 32 unique L2s, with 4 representative
  rows/map and 2 archived-collision rows/map;
- one 801-step episode reproduced rollout and canonical-BC means with exact
  max delta `0` and produced a 13D critic input;
- one known BC-collision episode terminated after 447 steps, had exactly 100
  nonzero collision rewards, and matched the terminal-only discounted return
  at the window start within `1.8e-8`;
- one synthetic 801-step framewise recurrent forward/backward changed both GRU
  and head gradients, took about 1.96 seconds locally, and allocated about
  322 MiB of CUDA memory.

The rejected-update test primes nonempty Adam moments and verifies bit-exact
restoration of actor tensors, `exp_avg`, `exp_avg_sq` and step counters before
halving both parameter-group LRs. The critic still completes three epochs.

## Execution boundary

The initial implementation commit is
`8b8f01cbf6b302a5916c137d53e2869a2f371ba3`. The final reviewed
execution-source commit is
`1c096c235e8c1a664ee2782cd8dd2f41c6988b11`. The authoritative learner must
use a clean archive from that pushed commit, an immutable B7 RunPlan,
remote `DISPLAY=:1`, the physical GPU lock, and isolated output/cache paths.
No seed1 learner or candidate evaluation existed at this implementation
boundary. No code after the final execution-source commit may enter the B7
source archive.

Before RunPlan creation, staging review added one infrastructure-only fix: the
RunPlan now freezes the inputs-archive digest and the immutable 288 BC-row file
digest in addition to Task-8, metadata, BC and source identities.

## First staged-attempt remediation

The first clean staged attempt passed the four-map production smoke and
accepted iteration 1 (`safe_kl=0.001707`, `rollout_kl=0.001748`). It then
failed closed during the iteration-2 actor backward because the isolation
assertion observed critic gradients left populated by iteration 1's final
critic minibatch. The actor loss had no critic path; the stale gradients had
not been cleared before the next actor-isolation check.

The fix calls `critic_optimizer.zero_grad(set_to_none=True)` before every actor
backward and retains the original assertion. A consecutive-iteration regression
now deliberately starts iteration 2 with populated critic gradients and proves
that one actor step plus all three critic epochs complete. Both B7 programs and
all nine legacy compatibility programs passed after the fix. No candidate or
evaluation was produced by the failed attempt, and it is not resumed.

Compact evidence is retained at
`docs/ppo/evidence/b7_stale_critic_gradient_20260714/`.
