# End2Race PPO V1.3-E CPU Evaluation Correction

**Active protocol revision:** `2`

## 1. Purpose

V1.3-E is an evaluation-only continuation of V1.3-D. It does not train another
actor and does not change any PPO hyperparameter.

V1.3-D correctly stopped with `STOP_PROTOCOL_DRIFT`: its fresh BC evaluation
ran on CUDA and produced `22 collision / 234 follow / 344 overtake`, while the
locked reference `21/233/346` was generated by the historical V1.2 evaluator
with `EVALUATION_DEVICE = torch.device("cpu")`.

The correction in V1.3-E is to restore the historical canonical evaluator's
execution semantics: persistent spawned CPU workers, one Torch intra-op thread
per worker, and one model load per worker. The three fixed V1.3-D U8 actors,
the 600 Austin scenarios, and all product thresholds remain unchanged.

Revision 1 attempted to express the CPU correction through `evaluate.sh`. It
was stopped after 200/600 BC rows because that shell evaluator launches a new
Python process and reloads the model for every scenario; each process also used
multiple Torch threads. It had no aggregate result and evaluated no candidate.
The partial evidence is recorded in `ABORTED_PREFLIGHT.json`. Revision 2 uses a
persistent worker pool equivalent to the historical baseline generator.

This continuation is explicitly exploratory because it was designed after the
V1.3-D device mismatch was observed. It cannot promote a model or consume a
holdout.

## 2. Immutable inputs

Canonical BC:

```text
pretrained/end2race.pth
sha256 b5a1360fee18c2875185a3d23ab21cbdd8a4cdb2e94639433a148f34809ac5e4
```

Fixed candidate actors:

| Seed | Source checkpoint | SHA-256 |
|---:|---|---|
| 20260735 | `runs/ppo/v1_3_d_seed20260735/checkpoints/end2race_ppo_v1_3_d_u0008_s20260735.pth` | `02351fc34fc6010dd0b2507bc11b25b1e1b10e6f0037646c549e968083ab5ce7` |
| 20260736 | `runs/ppo/v1_3_d_seed20260736/checkpoints/end2race_ppo_v1_3_d_u0008_s20260736.pth` | `e98976e42e8b55c1bfcfc637b43047efdbdf4c3288675c3040c20f5da91800d0` |
| 20260737 | `runs/ppo/v1_3_d_seed20260737/checkpoints/end2race_ppo_v1_3_d_u0008_s20260737.pth` | `b56f9998e7e2f149fbf19c9cb8eb7657c7f0baed29af1ef837896dfec28f68d5` |

Each actor must remain a strict 12-key `End2Race` state dict. Candidate copies
receive V1.3-E basenames only to isolate evaluation outputs; their bytes and
hashes must equal the source checkpoints.

## 3. Frozen evaluation contract

```text
device             = CPU
worker model load   = once per persistent worker
Torch threads       = 1 per worker
process start       = spawn
map                = Austin
collision_scope    = ego
workers            = 4
num_startpoints    = 50
ego_idx_offset     = 0
noise              = 0.0
sim_duration       = 8.0
render             = false
save_trace         = false
episodes           = 600
unique scenarios   = 600
error              = 0
```

No evaluation may be retried, merged, repaired, or partially reused.

## 4. Execution order

1. Copy BC and the three fixed candidates to their unique V1.3-E basenames and
   verify SHA-256 plus strict 12-key loading.
2. Evaluate the BC once on CPU.
3. Require exact BC counts `21 collision / 233 follow / 346 overtake / 0 error`.
4. If and only if the BC gate passes, evaluate the three candidates serially in
   seed order `20260735, 20260736, 20260737`.
5. Run `ppo_experiments/v1_3_e/aggregate_results.py` once all required outputs
   exist.

Every evaluation uses the same command shape:

```bash
CUDA_VISIBLE_DEVICES="" python \
  ppo_experiments/v1_3_e/evaluate_cpu_pool.py \
  --model-path <checkpoint> \
  --workers 4 \
  --output <results_multi.json>
```

The log must contain `EVAL_DEVICE=cpu`, `TORCH_NUM_THREADS=1`,
`EVALUATOR=persistent_spawn_pool`, the 600-segment start marker, and `error: 0`.

## 5. Locked product gate

For every one of the three fixed candidates, pair rows by scenario ID against
the fresh CPU BC and require all of:

```text
G = fixed_collision - new_collision >= 5
candidate collision <= 16
candidate overtake >= 340
mean speed ratio to BC >= 0.99
mean distance ratio to BC >= 0.99
```

The cross-seed gate is `3/3`. Mean, median, or a best seed cannot replace it.

## 6. Terminal verdicts

- `PASS_EXPLORATORY_CPU_DEVELOPMENT`: BC matches and all three candidates pass
  every product gate. This only authorizes a separately preregistered new
  holdout; it does not authorize selection or promotion.
- `FAIL_NO_STABLE_IMPROVEMENT`: at least one fixed candidate fails a product
  gate. The physical-Gaussian repair stabilized PPO updates but did not produce
  the required cross-seed driving improvement.
- `STOP_CPU_BASELINE_MISMATCH`: the CPU BC does not reproduce `21/233/346`.
- `INVALID_EVALUATION`: missing, malformed, non-600-row, non-ego, nonfinite, or
  otherwise invalid evaluation evidence.

In every verdict:

```text
selection_performed = false
holdout_performed = false
promotion_performed = false
canonical BC remains the deployment recommendation
```
