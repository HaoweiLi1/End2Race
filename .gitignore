__pycache__

build/
install/
.vscode/
.claude/
.codex/
.agent*
# .agents/ holds the agent handoff and repo contract; it is tracked on purpose.
!.agents/
!.agents/**
Dataset_*/
eval_results/*
# The scratch-area README explains where evaluation output must be filed.
!eval_results/README.md
logs/
# Experiment artifacts (models, npz, releases) are large and live on the
# remote host. Only the index, migration record and job runner are tracked.
Experiments/*
!Experiments/INDEX.md
!Experiments/PATH_MIGRATION.tsv
!Experiments/runner.py
HANDOFF*
